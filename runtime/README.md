# Runtime Assets

This directory stores local runtime assets for the desktop agent.

Expected target runtime:

- `runtime/llama.cpp/`: bundled llama.cpp executables.
- `runtime/models/minicpm-v4.6/`: MiniCPM-V 4.6 F16 GGUF model files and manifest.

Large binaries are intentionally not committed.
